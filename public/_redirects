# Serve pre-rendered HTML for key SEO pages (generated by postbuild script).
# Exact-path rules must come before the SPA catch-all.
/news                   /news/index.html                200
/insights               /insights/index.html            200
/business-intelligence  /business-intelligence/index.html  200
/insights/services      /insights/services/index.html   200

# SPA catch-all: all other routes return index.html with 200 (not 404).
/*    /index.html    200
